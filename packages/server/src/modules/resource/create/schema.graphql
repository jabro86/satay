input AttributeInput {
  name: String!
  type: String!
  required: Boolean!
  unique: Boolean
}

input ContentInput {
  attributes: [AttributeInput!]!
}

input CreateResourceInput {
  serviceName: String!
  name: String!
  content: ContentInput!
}

type Mutation {
  createResource(input: CreateResourceInput!): Boolean!
}
